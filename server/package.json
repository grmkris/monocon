{
  "name": "monocon",
  "module": "index.ts",
  "scripts": {
    "dev": "bun server.ts",
    "build": "bun build ./server.ts --outdir ./build && cp -r ../ui/dist/* ./build/"
  },
  "devDependencies": {
    "@types/minimist": "^1.2.2",
    "bun-types": "latest",
    "prettier": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "type": "module",
  "dependencies": {
    "minimist": "^1.2.8",
    "zod": "^3.22.2"
  }
}
